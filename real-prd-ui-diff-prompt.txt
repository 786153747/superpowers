请严格按当前仓库的 superpowers 工作流执行。

用户提供的需求文档是 `prd/后市场订单库存功能需求规格说明书.md`，现有 UI 项目是 `ruoyi-ui/`。

你的任务只有一个：
1. 先执行 `prd-diff-scan`
2. 对 PRD 与当前 UI 实现做逐页面差异分析
3. 必须把结果保存到 `docs/plans/YYYY-MM-DD-后市场订单库存-diff.md`
4. 不要进入 brainstorming、plan 或编码
5. 输出中必须明确给出生成的 diff 文档路径

注意：
- 这是基于当前真实项目的实际运行，不是示例或临时目录
- 如果某些 PRD 页面在 `ruoyi-ui/` 中不存在，必须在 diff 文档中明确标记为缺失/阻塞
- 需要按页面清单、字段对比、9维度对比、差异清单、Blockers 的结构输出并落盘
