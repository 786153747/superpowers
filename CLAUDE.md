# Claude Code 工作规则

> 先澄清、后设计、再计划、最后实现。关键决议必须落盘，不能只存在于对话里。

---

## 1. 工作流程

修改仓库文件时，按以下顺序执行：

1. **prd-diff-scan** — PRD / UI 图差异扫描（有 PRD/需求文档/原型图/截图时必做，单独执行）
2. **brainstorming** — 需求澄清 + 设计确认（读取已有的 diff 文档）
3. **writing-plans** — 拆分实施计划
4. **实现** — 按计划逐步编码
5. **审查** — 代码审查

**每一步单独执行**，确认输出合格后再进入下一步。不要在一个 skill 里完成多个阶段。

用 `Skill` 工具调用 skill：`Skill("superpowers:prd-diff-scan")`、`Skill("superpowers:brainstorming")`。禁止用 `Task` 工具调用 skill。

纯聊天、纯概念问答不受此规则约束。

### Skill 路由（收到用户消息后先检查）

用户提供了 PRD / 需求文档 / 原型图 / UI 截图，且 `docs/plans/*-diff.md` 不存在？→ **必须先调 `prd-diff-scan`**，不管用户要求什么（即使用户说"用 brainstorming"）。

用户**不需要显式点名** `prd-diff-scan`。只要消息里出现以下任一类信息，也必须自动进入 `prd-diff-scan`：

- 直接给出 PRD / 需求文档路径，例如：`prd/xxx.md`、`docs/xxx-prd.md`
- 直接给出现有实现目录，例如：`ruoyi-ui/`、`src/`、某个前端/后端模块路径
- 组合表达，例如："需求文档在 `prd/...`，UI 项目在 `ruoyi-ui/`"、"按这个 PRD 对当前项目做差异分析"

也就是说：**用户只提供路径和目标，也算完整的 diff-scan 触发信号，不需要再显式说“使用 prd-diff-scan skill”。**

出现以下中文表达，也视为必须先走 `prd-diff-scan`：

- “对照 PRD 和 UI 图看看差异”
- “按原型图核对现状”
- “做页面差异分析 / 差异扫描 / 页面核对”
- “看截图和现在实现差了什么”
- “需求文档在 `prd/...`，UI 项目在 `ruoyi-ui/`”
- “按这个 PRD 对当前项目跑一遍差异分析”

### 文档创建权限

每种文档只能由对应的 skill 创建：

| 文档类型 | 只能由此 skill 创建 | 其他 skill 禁止创建 |
|----------|---------------------|---------------------|
| `*-diff.md` | prd-diff-scan | brainstorming ❌ writing-plans ❌ |
| `*-design.md` | brainstorming | prd-diff-scan ❌ writing-plans ❌ |
| `*-detail-design.md` | brainstorming（用户明确要详细设计时） | prd-diff-scan ❌ writing-plans ❌ |
| `*-plan.md` | writing-plans | brainstorming ❌ prd-diff-scan ❌ |
| `*-db-design.md` | writing-plans | brainstorming ❌ prd-diff-scan ❌ |

违反此规则 = 流程失败，必须回退。

---

## 2. PRD 差异扫描

当用户提供了 PRD、原型图、截图或需求文档时：

- 必须先做 PRD / 现状差异扫描，再谈设计
- 如果同时给了 **PRD + UI 图 / 原型图 / 截图**，必须做 **PRD ↔ UI 图 ↔ 当前实现** 三方对比，而不是只看代码现状
- 差异扫描结果保存到 `docs/plans/YYYY-MM-DD-<主题>-diff.md`
- 有 Blocker 未解决时，不得进入设计或实现
- "建议决议"是你的推荐，用户确认后才是"差异决议"
- UI 图只能证明**可视信息**；图片无法证明的数据来源、状态流转、权限、接口行为，不得脑补，必须标记为 `Blocker/待确认`
- 如果 PRD 中引用了**外部原型图 URL / 无法访问的截图 / 失效图片链接**，必须在 diff 文档中单列“原型图证据状态/证据不足项”，并把依赖这些图片才能确认的页面或维度标记为 `Blocker/待补图`
- 没拿到可访问的原型图时，不得把“PRD vs 原型图 vs 当前实现”伪装成已完成三方核对；必须明确写成“PRD vs 当前实现已核对，原型图证据不足”

### 合格的差异扫描必须包含：

1. **受影响页面清单**（表格：页面名 / 实现文件 / 核对状态）
2. **每个页面的 UI 可视要素对比**（若提供原型/截图：页面标题 / 筛选区 / 表格结构 / 表单布局 / 按钮文案 / 状态标签 / 提示文案 / 空态）
3. **每个页面的字段对比**（表格：查询条件 / 表格列 / 表单字段 / 行操作按钮，各列对比 PRD 要求 vs UI 图 vs 当前实现）
4. **每个页面的 9 维度对比**（字段类项不计入 9 维度；表格：交互行为 / 触发事件 / 数据来源 / 数据流向 / 字段联动 / 状态流转 / 权限与角色 / 默认值/排序/回显 / 下拉字典来源）
5. **每个页面的验收点**（正向 / 空态 / 异常态 / 权限态；未核对则计入 blocker）
6. **差异清单 + Blockers**
7. **用 Write 工具保存到 `docs/plans/` 目录**（不能只输出在聊天中）
8. **若原型图不可访问，必须列出证据不足清单**（表格：图片引用 / 所属页面 / 不足原因 / 影响维度 / 是否阻塞）

### 不合格的差异扫描（禁止）：

以下输出不算差异扫描，必须重做：

- "前端部分实现，后端尚未实现"
- "UI 组件已创建，API 使用 Mock 数据"
- "页面看起来差不多"
- "截图里大概是这个意思"
- "PRD 里有原型图链接，所以我默认原型图与当前实现一致"
- "图片打不开，但我先按猜测补全三方对比"
- 任何没有页面级对比表格的模块级总结

---

## 3. 文档规则

所有设计、计划、测试、审查文档统一放在 `docs/plans/`：

- 详细设计: `*-detail-design.md`
- 数据库设计: `*-db-design.md`
- 测试清单: `*-test-cases.md`
- 实施计划: `*-plan.md`
- 差异扫描: `*-diff.md`

### 详细设计怎么写

- 用户要的是**后端详细设计**，就去看 `spec/backend/java/detail-design-template.md`
- 用户要的是**前端详细设计**，就去看 `spec/frontend/vue/detail-design-template.md`
- 前后端都要时，**分两份文档写**，不要混成一篇
- 文档不能只写在聊天里，**必须落盘**到 `docs/plans/`
- 推荐文件名：
  - `docs/plans/YYYY-MM-DD-<主题>-backend-detail-design.md`
  - `docs/plans/YYYY-MM-DD-<主题>-frontend-detail-design.md`
- 写的时候尽量用白话，少讲空话，多写具体的文件、接口、字段、规则、验证方式
- 如果项目里还没有 `spec/`，先补一份再继续；不要跳过模板直接自由发挥

文档头部包含：

```md
## 需求输入

- PRD: [路径]
- 项目规范: [路径]
```

---

## 4. 确认规则

以下场景必须等用户确认后再继续：

- 需求不清
- 多种方案可选
- 设计完成要进入计划
- 计划完成要进入实现
- 有破坏性变更

未确认前不得编造业务规则，不得自行拍板。

### 自动测试 / 非交互例外

如果用户**明确**说明这是自动测试、CI、批处理或非交互场景，并且同时明确授权了以下几件事：

- 没有 blocker 时，默认采用推荐方案
- 默认同意继续下一步
- 默认同意把文档落盘到 `docs/plans/`

则允许把原本要逐轮确认的步骤串起来执行，直接产出并落盘文档。

但有两个前提不能破：

- **只在用户明确授权时才生效**，不能自己脑补成自动模式
- **只要还有 blocker / 关键冲突 / 需求不清**，就必须停下来，不能硬着头皮继续

### 每步一轮交互

brainstorming 的每个 Step 完成后，必须**停下来等用户回复**，不得在一个回合中连续完成多个 Step。
一个回合 = 一次模型输出。每次输出最多推进一个 Step。

上面这条在“自动测试 / 非交互例外”成立时，可以按用户授权合并执行。

---

## 5. 编码原则

- 只改与当前目标直接相关的内容
- 不顺手修无关问题
- 不引入与当前任务无关的重构
- 先写测试、再写实现（TDD）
